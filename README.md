# QML-for-binary-classification
Implement quantum machine learning (QML) using Pennylane on the Iris and MNIST datasets.

## Iris
For Iris dataset, only the first 100 samples (i.e. the first two catagories) are used. 2 qubits and 4 layers are implemented to execute the classification task. Each layer contains three rotational gates R_X 
